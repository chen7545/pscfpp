namespace Pscf {

   /**
   * Real periodic fields, SCFT and PS-FTS (CPU).
   *
   * All field theoretic simulation (FTS) methods implemented in this
   * namespace rely on a partial saddle point approximation, which
   * allows the use of real chemical potential fields.
   *
   * Source code files in this namespace may use "using namespace" 
   * declarations that allow use of names from namespaces Util, 
   * Pscf::Prdc, and Pscf::Prdc::Cpu without explicit namespace
   * qualifier prefixes.  Names from namespace Pscf::Rp may also 
   * be used, but only with an explicit Rp:: namespace qualifier. 
   */
   namespace Rpc {

      /**
      * \defgroup Pscf_Rpc_Module Pscf::Rpc - Real Periodic Fields (CPU)
      *
      * SCFT and PS-FTS with real periodic fields on a CPU (namespace Pscf::Rpc)
      *
      * \ingroup Pscf_NS_Module
      */

   }

}
